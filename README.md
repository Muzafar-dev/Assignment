After cloning or downloading the project files into you local</br>
Use the following command
npm install
After installing the dependencies, Use the following command
npm start
This will start the http://localhost:3001
